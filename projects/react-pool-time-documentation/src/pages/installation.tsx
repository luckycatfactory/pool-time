import React from 'react';

const Installation = React.memo(() => (
  <div>
    <h1>Installation</h1>
    <p>You&apos;ll want to run some stuff.</p>
  </div>
));

Installation.displayName = 'Installation';

export default Installation;
