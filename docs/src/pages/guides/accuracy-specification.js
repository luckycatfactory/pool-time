import React from 'react';

const AccuracySpecification = React.memo(() => <h1>Accuracy Specification</h1>);

AccuracySpecification.displayName = 'AccuracySpecification';

export default AccuracySpecification;
